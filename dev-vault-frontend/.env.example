# Public configuration only. Never put passwords, signing keys or service tokens here.
# Local development uses the Vite proxy to Django on 127.0.0.1:8000.
VITE_API_BASE_URL=/api/v1/
# Production: use your exact HTTPS backend /api/v1/ URL and configure its CORS allowlist.
